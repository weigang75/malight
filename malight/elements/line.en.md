<!-- Generated by tools/gen_docs.py from the source docstrings.
     Do not edit by hand: edit the docstrings in line.py and re-run. -->

# line.py

[简体中文](line.zh.md) ｜ **English** ｜ [← Back to README](../../README.en.md)

LineElement, created by pen.line.

---

## Classes and methods

### `LineElement`

Line element.

| Method | Description |
|---|---|
| `bbox()` | Bounding box. |
| `set_start(value)` | Set start; the same as `update(start=value)`. |
| `get_start()` | Read the current raw start attribute. |
| `set_end(value)` | Set end; the same as `update(end=value)`. |
| `get_end()` | Read the current raw end attribute. |

---

## Full example

Run it directly in PyCharm, or from the command line: `python malight/elements/line.py`

```python
if __name__ == "__main__":
    import os

    from malight import (Malight, ColorName, StrokeCap, StrokeJoin, DashStyle,
                         VectorEffect)

    _out = os.path.abspath(os.path.join(os.path.dirname(__file__),
                                        "..", "..", "output"))
    pen = Malight(os.path.join(_out, "demo_line"), width=620, height=360)
    pen.set_background_color(ColorName.WHITESMOKE)

    # 1) A basic line from start to end
    pen.line((40, 60), (580, 60), stroke_color=ColorName.NAVY, stroke_width=3)

    # 2) Thick round-capped line, the usual pick for capsule and hand-drawn strokes
    pen.line((40, 120), (580, 120), stroke_color=ColorName.TOMATO,
             stroke_width=16, stroke_cap=StrokeCap.ROUND)

    # 3) Dash presets as enums; no more hand-written "8 4"
    pen.line((40, 180), (580, 180), stroke_color=ColorName.SEAGREEN,
             stroke_width=4, stroke_style=DashStyle.DASH_DOT_DOT)

    # 4) Dash offset staggers one dash pattern for flow or progress effects
    for i in range(6):
        pen.line((60 + i * 90, 220), (60 + i * 90, 280),
                 stroke_color=ColorName.SLATEBLUE, stroke_width=6,
                 stroke_style=DashStyle.DASHED, dash_offset=i * 4)

    # 5) Marching-ants animation, visible in a browser
    flow = pen.line((40, 320), (580, 320), stroke_color=ColorName.CRIMSON,
                    stroke_width=4, stroke_cap=StrokeCap.ROUND)
    flow.animate_dash_flow(dur=1.5, dash="10 8")

    # 6) Keep the stroke width when the canvas scales, as diagrams and icons need
    ln = pen.line((40, 345), (580, 345), stroke_color=ColorName.INDIGO,
                  stroke_width=2, vector_effect=VectorEffect.NON_SCALING_STROKE)
    print("line bbox:", tuple(round(v, 1) for v in ln.bbox()))

    # 7) stroke_join controls the corner between two segments
    pen.polyline([(40, 292), (90, 262), (140, 292)],
                 stroke_color=ColorName.CHOCOLATE, stroke_width=8,
                 stroke_join=StrokeJoin.ROUND)

    # 8) Partial update: only the end point changes
    l2 = pen.line((420, 250), (500, 250), stroke_color=ColorName.GOLD,
                  stroke_width=6, stroke_cap=StrokeCap.ROUND)
    l2.update(end=(560, 290))
    print("line d/coords after update:", l2.node.attribs)

    pen.finish()
```

---

## Sibling modules

[base](base.en.md) ｜ [circle](circle.en.md) ｜ [clippath](clippath.en.md) ｜ [ellipse](ellipse.en.md) ｜ [group](group.en.md) ｜ [image](image.en.md) ｜ [link](link.en.md) ｜ [marker](marker.en.md) ｜ [mask](mask.en.md) ｜ [path](path.en.md) ｜ [pattern](pattern.en.md) ｜ [polygon](polygon.en.md) ｜ [polyline](polyline.en.md) ｜ [rect](rect.en.md) ｜ [svggroup](svggroup.en.md) ｜ [svgimage](svgimage.en.md) ｜ [symbol](symbol.en.md) ｜ [text](text.en.md) ｜ [textpath](textpath.en.md) ｜ [use](use.en.md)
